run:
  # Reuse baseline parameters and only keep soft-specific overrides here.
  base_config: config/base.yaml

soft_outside:
  enabled: true
  tau: 0.2   # smoothing temperature; try 0.05, 0.1, 0.2, 0.5